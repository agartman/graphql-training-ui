/*import { PeopleList } from "./PeopleListWithData"

import React from "react"
import { shallow } from "enzyme"

it("should render a PeopleList component correctly", () => {
  const data = { getPeople: [{ name: "BB8" }, { name: "Leia" }] }
  const wrapper = shallow(<PeopleList data={data} />)

  expect(wrapper).toMatchSnapshot()
})
*/
